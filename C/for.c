#include <stdio.h>

int main(){
    int clave, i;
    printf("-9 termina el programa \n");
    clave=1;
    for(i=0, i<8; i++){
        if (clave==-9) continue;
        scanf("%d", &clave);
        printf("clave = %d \n",clave);

    }
    printf("END i=%d clave= %d \n",o,clave);
return 0;
}
